<h1>Patient Show</h1>

<h1><%= @patient.name %></h1>

<% @patient.appointments.each do |a| %>
	<p><%= a.appointment_datetime.strftime("%B %d, %Y at %k:%M") %></p>
	<p><%= link_to a.doctor.name, doctor_path(a.doctor.id) %></p>
<% end %>