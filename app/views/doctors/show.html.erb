<h1>Doctor Show</h1>

<h1><%= @doctor.name %></h1>

<h3><%= @doctor.department %></h3>

<% @doctor.appointments.each do |a| %>
	<p><%= a.appointment_datetime.strftime("%B %d, %Y at %k:%M") %></p>
	<p><%= link_to a.patient.name, patient_path(a.patient.id) %></p>
<% end %>